import AddBlog from "./AddBlog";

const title = "Add Prompt";
const description = "Prompt creator for adding new blogs.";

export const metadata = {
  title,
  description,
  openGraph: {
    description,
  },
};

export default AddBlog;
